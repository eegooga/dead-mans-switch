services:
  dead-mans-switch:
    build: .
    container_name: dead-mans-switch
    restart: unless-stopped
    environment:
      TZ: Europe/Moscow
    env_file:
      - .env
    volumes:
      - ./messages:/app/messages
      - ./logs:/app/logs
      - ./lock:/app/lock
